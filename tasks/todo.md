# Union Background Scope Fix Plan

- [x] Trace why the homepage `Union` background is appearing on analyzing and details screens.
- [x] Scope the `Union` background so it only renders on homepage-style screens.
- [x] Build the project to verify the shell fix, then commit and push the completed feature.

# Review

- Scoped the homepage `Union` background to homepage-style shells only, so it no longer appears on the analyzing or details pages.
- Verified the shell fix with `npm run build`, which completed successfully.
