# Details Page Video And Gauge Plan

- [x] Review the current report page implementation and choose an official gauge library for the confidence UI.
- [x] Add the selected gauge dependencies and replace the custom confidence graph with a cleaner library-backed gauge.
- [x] Make the report hero media playable in an overlay and connect discrepancy ranges so users can inspect flagged moments.
- [x] Build the project to verify the details page feature updates, then commit and push the completed feature.

# Review

- Installed the MUI X Gauge stack and replaced the custom confidence SVG with a library-backed gauge for a cleaner report UI.
- Made the report hero media playable in an overlay with direct-video playback, common-provider embed playback, and discrepancy jump points based on the evidence ranges.
- Verified the details page updates with `npm run build`, which completed successfully.
